# BOSS 求职工作台开发路线图

## 当前阶段

项目目前处于 Beta 内测稳定化阶段。核心求职流程已经闭环，阶段 1、阶段 2、阶段 3、阶段 4、阶段 5 的首轮治理已经完成：

- 简历解析与优化
- BOSS 岗位抓取与多维筛选
- JD 详情补全
- 企业工商信息与公司尽调
- 行业趋势分析
- 岗位排序与权重配置
- 招呼语生成与 PDF 导出
- 岗位状态、决策标签、企业黑名单、岗位池质量治理
- 前端类型检查、前后端测试、构建与依赖审计

新增落地能力：

- 顶部全流程状态接入系统健康检查，可展示 Python、pnpm、前端构建、数据目录、AI、百度搜索、工商 API、BOSS 登录缓存状态。
- 失败任务支持基于原始参数创建重试任务，前端恢复中心可直接触发。
- 岗位池接入抓取批次维度，记录关键词、城市、筛选条件、抓取时间，并支持质量面板展示。
- 岗位、尽调、排序结果支持 JSON / CSV 导出。
- 尽调导出增加证据摘要，整合工商基础项、搜索链接、行业机会、行业弱点与风险。
- 排序模块增加权重模板、排序解释、简历缺口、公司风险解释和下一步建议。
- 仪表盘成为默认首页，集中展示岗位总数、待补 JD、待尽调公司、推荐岗位、风险岗位、疑似过期岗位。
- 完整数据维护接入设置面板，支持全量数据备份/恢复、数据保留策略、维护日志、存储状态查看。
- 岗位模块支持保存和复用常用搜索条件，包含关键词、城市、页数、BOSS 多维筛选和本地筛选条件。
- 智能求职助理接入仪表盘，支持投递策略、JD 质量识别、简历反推建议、面试准备、跟进提醒、风险解释。
- 阶段 3/4/5 剩余项已补齐：SQLite 快照迁移/回滚、外部 API 调用日志、旧数据归档、岗位视图切换、PDF 模板推荐、简历版本对比、智能助理结果持久化和 AI 深度报告。
- 作品级治理首批已接入：岗位状态历史、操作审计、删除归档与恢复、任务幂等键、批次质量率、岗位对比。
- 仪表盘已增加流程质量分、当前阶段、阻断项和上线体检；尽调证据已区分工商事实、搜索复核和 AI 辅助判断。
- 上线安全治理已开始落地：AI、百度搜索、工商 API 密钥支持本地加密存储，并兼容旧明文配置自动迁移。
- 生产/演示模式隔离已接入：生产模式默认禁止示例抓取，避免演示数据混入真实岗位池。
- 求职转化复盘已接入：统计触达率、面试率、拒绝率、批次转化表现和复盘建议。
- 用户求职偏好已接入设置面板：可记录稳定性、薪资、成长、匹配度、规避行业和偏好城市。
- 本地访问保护已接入：默认仅允许本机访问服务，远程访问需显式设置 `BOSS_WORKBENCH_ALLOW_REMOTE=true`。
- 用户偏好已进入排序解释：排序结果会展示稳定性、薪资、成长、匹配度、规避行业、偏好城市等偏好命中信号。
- 删除恢复已具备前端入口：设置维护区展示最近删除岗位，可直接恢复。
- AI 深度报告已接入用户求职偏好，报告输入和返回结果都会包含偏好命中信号。
- 上线检查脚本已新增：`python3 scripts/release_check.py` 可一键执行后端测试、前端校验和 Git 差异格式检查，`--dry-run` 可预览检查项。
- 密钥轮换说明已新增到 `docs/SECURITY.md`，覆盖本地加密主密钥、轮换步骤和发布前检查。
- 岗位池已具备 SQLite 主存储开关，设置页可将岗位主读写切换为 SQLite，并保留 JSON 回滚能力。
- 发布清单和安全审计已接入维护模块，覆盖质量门禁、明文密钥扫描和本地加密主密钥检查。
- AI 深度报告支持 Markdown / JSON / PDF 导出，便于岗位复盘、面试准备和长期归档。
- PDF 模板增加字体、密度、适用岗位和版式元数据，前端可基于岗位类型展示更清晰的模板选择。
- BOSS 抓取日志细化到搜索页、汇总和详情补全维度，维护日志可按 `boss_capture` / `boss_detail` 分类查看。
- 统一存储继续收口：岗位、尽调、排序、简历、维护日志与外部 API 调用日志均已接入 SQLite 通用键值表；旧版简历 JSON 和日志 JSONL 可在迁移时导入 SQLite，同时保留 JSON 回退副本。
- 仪表盘新增新手引导和复盘中心，覆盖配置、抓取、JD、尽调、排序、打招呼六步流程，以及批次/风险/缺失 JD 复盘建议。
- AI 深度报告支持前端预览导出入口和人工复核保存，后端保留人工版本历史。
- BOSS JD 详情失败任务支持按失败岗位 ID 重试，减少整批重复执行。
- 启动器增加启动前检查，覆盖 Python、pnpm、前端构建、数据目录和端口状态。
- 前端增加 Playwright 冒烟测试，覆盖工作台、设置、岗位、尽调、排序，以及简历预览 PDF 和 AI 深度报告 PDF 下载。
- 外部 AI、工商和百度搜索调用已统一接入瞬时失败重试、限流识别、熔断和结构化错误信息。
- Playwright 后端使用 `BOSS_WORKBENCH_TEST_MODE=1`，报告结果确定且不访问真实供应商。
- SQLite 增加 schema 版本、完整性检查、数据库备份和恢复预览，维护设置可查看数据库状态。
- 任务中心接口已接入，统一汇总运行中、失败、可重试和最近任务，便于前端恢复中心和后续桌面化复用。
- 投递时间线已接入，岗位求职状态变化会自动汇总到仪表盘，帮助用户持续跟踪触达、投递、面试和拒绝记录。
- 求职周报已接入仪表盘，汇总最近 7 天新增岗位、JD 完整度、尽调、触达、面试、拒绝和失败任务。
- 智能打招呼工作台开发文档已新增到 `docs/AUTO_GREETING_WORKBENCH.md`，基于 GitHub 相关项目调研，明确采用半自动、可审核、可恢复、可追踪的受控触达方案。
- 智能打招呼工作台阶段 A 已落地首版：候选岗位筛选、招呼语草稿生成、发送前校验、dry-run 审计记录和前端工作台入口。
- 智能打招呼工作台阶段 B 已落地首版：人工确认发送、批量确认选中草稿、每日/单批限额、校验失败拦截、岗位状态/投递时间线/任务中心联动。
- 智能打招呼已增加真实浏览器发送首版：自动打开 BOSS 岗位详情、点击立即沟通、粘贴并发送；遇到登录失效、验证码、风控或页面结构变化会停止并记录失败原因。
- 自动发送稳定性继续完善：支持发送间隔、单批/今日上限、风控后暂停剩余岗位，以及失败任务按失败岗位重试。
- 自动发送控制台继续完善：增加真实自动发送总开关、发送前预检、暂停/继续/终止、频率模板和触达复盘统计。
- 自动发送真实可用性继续完善：增加选择器健康检查、人工验收步骤和 24/48/72 小时待跟进提醒。
- BOSS 打招呼链路增加人工验收记录，可保存验收人、结果、检查项和备注，用于上线前真实链路确认。
- 打招呼回复跟踪首版已接入，可记录 HR 回复类型、内容摘要和下一步动作，并进入打招呼统计。
- 招呼语效果分析已接入：按岗位类型汇总发送数、回复率、积极回复率和平均话术长度，辅助优化后续模板。
- 新手引导升级为可量化清单，展示完成进度、下一步按钮和每步动作文案。
- 失败恢复中心升级为按钮式恢复动作，会按登录、风控、网络、页面变化、数据缺失给出直接处理入口。
- 求职 CRM 看板已接入仪表盘，按待处理、已打招呼、已投递、面试中、已拒绝、已放弃分栏展示岗位。
- 求职 CRM 看板支持拖拽和下拉修改求职状态，更新后会同步岗位状态、投递时间线、求职漏斗和看板数据。
- 仪表盘新增 30 天趋势图，覆盖新增岗位、JD 完整度、尽调完成、打招呼发送、回复、积极回复和面试推进。
- 仪表盘新增数据质量体检页块，集中检查缺失 JD、重复岗位、疑似过期、黑名单、工商名称缺失、低质量 JD 和未排序岗位。
- AI 深度报告新增提示词版本记录，保存提示词版本、岗位上下文摘要、偏好信号数量和当时的反馈快照，便于后续复盘 AI 输出。
- 自动打招呼新增最终确认摘要，发送前汇总本批岗位、BOSS 链接、话术长度、今日剩余额度和风险项。
- 数据质量治理继续细化：仪表盘支持一键给缺失 JD、低质量 JD、疑似过期岗位补维护标签，后续可按标签执行补 JD、归档或重排。
- AI 深度报告新增质量评分，检查是否结合投递策略、JD、公司风险、行动建议和用户反馈。
- 简历 PDF 密度参数已真正进入导出/预览生成逻辑，可影响字号、行距、段落间距和页面留白。
- 设置维护区新增上线报告导出与完整备份恢复演练，导入前会预览将恢复和覆盖的文件数量。
- 上线质感继续完善：维护区增加脱敏完整备份和上线人工验收清单，便于演示、交付和真实使用前逐项确认。
- 产品成熟度继续完善：任务中心增加失败恢复分组，维护区增加版本说明和一键上线体检入口。
- 报告、审计和存储治理继续完善：AI 深度报告支持章节级人工编辑，维护区支持真实依赖审计和 SQLite 迁移向导。
- AI 反馈闭环首版已接入：排序、尽调、JD 分析、AI 深度报告可标记“有用/需改”，后台统一沉淀反馈摘要，仪表盘展示反馈总量和需改数量。
- PDF 视觉打磨首轮已完成：AI 深度报告强化标题层级、分隔线和正文节奏；简历 PDF 保持中文字体稳定，并通过更舒展的行距降低密集感。
- AI 反馈反哺首版已接入：排序“需改”反馈会基于公司分/匹配分轻量修正下一轮权重，AI 深度报告生成时会带入近期反馈摘要和需改备注。
- PDF 真实渲染回归已接入：维护区可运行样例简历 PDF 和 AI 报告 PDF 的页面转图检查，验证页面非空白、尺寸正常、主要内容已渲染。
- AI 偏好画像已接入：系统会汇总反馈域、需改原因、公司风险/简历匹配倾向和岗位类型信号，作为后续个性化排序和报告优化基础。
- PDF 真实渲染检查已支持前端缩略图展示，维护区可直接看到样例简历和 AI 报告第一页渲染结果。

最近验证结果：

- 后端全量测试：`182 passed`
- 前端校验：`pnpm validate` 通过，包含类型检查、9 个测试文件 / 18 个测试、生产构建。
- Playwright 端到端：`5 passed`，覆盖工作台、设置、岗位、尽调、排序、简历 PDF 预览和 AI 深度报告 PDF 下载。

当前重点不再是单纯增加功能，而是提升真实使用中的稳定性、可恢复性、可信度和长期数据管理能力。

## 作品级治理：第一轮完成

- 已完成：岗位求职状态和决策标签写入 `status_history`，支持按岗位查看状态变化记录。
- 已完成：岗位状态、决策、黑名单、合并、删除、恢复等关键操作写入维护审计日志。
- 已完成：删除岗位先归档到 `data/jobs/deleted_jobs.json`，支持通过恢复接口找回，避免误删不可逆。
- 已完成：抓取和 JD 补全任务增加幂等键，重复点击不会重复创建运行中的同类任务。
- 已完成：岗位批次质量增加 JD 完整率、过期率、风险率。
- 已完成：岗位模块支持选择 2-5 个岗位进行薪资、JD、生命周期、求职状态和决策对比。
- 已完成：上线体检聚合健康检查、存储、备份、数据保留和近期错误日志。
- 已完成：敏感配置本地加密存储，覆盖 AI Provider、百度搜索和腾讯云工商 API。
- 已完成：生产模式默认关闭示例抓取；如需演示数据，需要设置 `BOSS_WORKBENCH_MODE=demo` 或 `test`。
- 已完成：求职漏斗复盘接口，覆盖触达、面试、拒绝、推荐岗位和批次转化表现。
- 已完成：用户偏好记忆接口与设置面板，作为后续个性化排序和 AI 建议的基础。
- 已完成：本地访问保护，默认阻止非本机 Host 请求。
- 已完成：用户偏好接入排序解释，排序结果可展示偏好命中信号。
- 已完成：删除岗位回收站在设置维护区可见，并支持一键恢复。
- 已完成：用户偏好接入 AI 深度报告，稳定性、薪资、成长、匹配度、规避行业、偏好城市会进入深度报告输入与返回。
- 已完成：发布前检查脚本与 dry-run 测试，覆盖后端测试、前端校验、Git 差异格式检查。
- 已完成：密钥轮换与发布前安全说明，记录在 `docs/SECURITY.md`。
- 已完成：岗位池 SQLite 主存储开关，主存储状态可在设置页查看与切换。
- 已完成：发布清单和安全审计接口，设置页展示安全审计摘要。
- 已完成：上线检查脚本增加本地明文密钥扫描。
- 已完成：尽调报告和排序结果接入 SQLite 通用键值表。
- 已完成：安全审计增加依赖审计准备度和导出隐私扫描。
- 已完成：外部供应商统一重试与熔断，区分网络、限流、供应商、鉴权和参数错误。
- 已完成：简历优化在 AI 返回结构化错误时正确进入规则兜底，避免空结果覆盖。
- 已完成：统一任务中心接口，前端仪表盘可展示运行、失败、可重试任务和最近任务。
- 已完成：失败恢复中心按登录/凭据、页面风控、网络供应商、页面结构变化、数据缺失等类型分组，并给出处理建议。
- 已完成：失败恢复中心增加按钮式恢复动作，可直接跳转到对应模块处理。
- 已完成：投递时间线接口和仪表盘展示，按岗位状态历史汇总最近求职推进记录。
- 已完成：求职 CRM 看板接口和仪表盘展示，按岗位求职状态分栏查看推进。
- 已完成：求职 CRM 看板支持拖拽改状态和下拉改状态，状态更新写入历史并刷新看板。
- 已完成：求职周报接入仪表盘，统计最近 7 天新增岗位、JD 完整度、尽调、触达、面试、拒绝和失败任务。
- 已完成：30 天趋势统计接入仪表盘，覆盖岗位扩池、JD 补齐、尽调、触达、回复、积极回复和面试推进。
- 已完成：数据质量体检接入仪表盘，按可行动问题给出数量、严重度、原因和跳转入口。
- 已完成：AI 输出反馈记录接口，覆盖排序、尽调、JD、打招呼、深度报告和 PDF 相关结果，为后续个性化权重、提示词复盘和质量治理提供数据基础。
- 已完成：排序反馈进入下一轮权重微调；深度报告提示词接收反馈摘要，优先补强被标记需改的维度。
- 已完成：AI 深度报告生成时记录提示词版本和反馈快照，仪表盘可查看最近提示词版本记录。
- 已完成：AI 深度报告质量评分，生成结果和独立接口都会返回可解释质量分与待补强项。
- 已完成：AI 反馈偏好画像接口和设置页摘要展示，用于识别用户更在意公司风险还是简历匹配。
- 已完成：新手引导增加进度条和每步动作，减少首次使用时的路径不确定。

## 后续治理方向

- 岗位、尽调、排序、简历与日志的核心读写已具备 SQLite 开关；JSON 副本仍保留为兼容和回滚保障，后续可逐步缩减旧副本依赖。
- API Key 已支持本地加密存储；本地访问保护、密钥轮换说明、脱敏完整备份和导出隐私扫描已接入。
- 增加更完整的真实人工验收脚本、端到端浏览器测试和失败恢复测试。
- 增加求职转化复盘、批次趋势分析、评分反馈和用户偏好记忆。
- 按 `docs/AUTO_GREETING_WORKBENCH.md` 推进智能打招呼模块，优先落地候选筛选、招呼语生成、发送前校验、dry-run、审计日志和人工审核工作台。

## 阶段 1：内测稳定性（首轮完成）

目标：真实用户每天可用，出错后能理解原因并快速恢复。

- 已完成：BOSS 登录状态提示已包含未登录、Cookie 失效、页面风控、网络失败等原因。
- 已完成：长任务状态已覆盖抓取、JD 获取、尽调、尽调刷新、排序等流程，并在顶部全流程状态展示。
- 已完成：失败任务保留失败原因、原始参数、建议动作，并支持创建重试任务。
- 已完成：一键健康检查覆盖 Python、pnpm、前端构建、API 配置、数据目录、BOSS 登录状态。
- 已完成：阶段 1 / 2 后端回归测试已新增，覆盖健康检查、重试、导出、批次、排序解释。
- 持续项：继续扩展端到端测试，重点覆盖真实浏览器抓取链路和失败恢复后的真实执行。
- 固定门禁：后端测试与 `pnpm validate` 作为提交前质量检查。
- 固定依赖：后端测试依赖 FastAPI/Starlette `TestClient`，依赖清单需包含 `httpx2`。

## 阶段 2：数据与决策质量（首轮完成）

目标：让用户知道结论从哪里来，为什么可信。

- 已完成：尽调报告已分层展示工商证据、搜索证据、AI 信号、用户备注，并支持证据刷新。
- 已完成：行业趋势已结合工商 API 返回的所属行业，交给 AI 进行行业优劣势分析。
- 已完成：排序结果增加推荐原因、公司风险解释、简历缺口、下一步建议和权重拆解。
- 已完成：权重模板增加均衡推荐、低风险优先、高匹配优先。
- 已完成：岗位池增加批次维度，包含来源、抓取时间、关键词、城市、筛选条件。
- 已完成：岗位、尽调、排序结果支持 JSON / CSV 导出，方便复盘和备份。
- 持续项：后续可继续补充高薪优先、成长优先、大厂优先等更细分模板，并允许用户自定义保存模板。

## 阶段 3：存储与长期维护（首轮完成）

目标：从 JSON 文件工具升级为稳定本地应用。

- 已完成：增加完整备份与恢复，支持导出/导入本地 `data` 目录内的业务数据文件。
- 已完成：增加数据保留策略首版，可预览并归档疑似过期岗位。
- 已完成：增加维护日志中心，备份、恢复、保留策略操作会写入本地事件日志并可在设置面板查看。
- 已完成：增加存储状态接口，展示当前 JSON / SQLite 主存储状态和 SQLite 文件路径。
- 已完成：统一错误码体系已覆盖主要 API 错误，前端恢复中心可展示失败原因和建议动作。
- 已完成：SQLite 快照迁移与 JSON 回滚接口已完成，迁移前后可查看存储状态。
- 已完成：旧聊天记录、失败任务归档已接入数据保留策略。
- 已完成：外部 API 调用日志已覆盖工商、百度搜索、AI 调用，并可在设置维护区查看。
- 已完成：SQLite 主读写已覆盖岗位、尽调、排序、简历和日志；历史简历 JSON 与日志 JSONL 迁移时会导入统一键值表，JSON 副本保留为兼容回退。
- 已完成：SQLite 增加 schema 版本记录、事务迁移、完整性检查、时间戳备份与恢复预览。

## 阶段 4：用户体验打磨（首轮完成）

目标：从功能完整变成顺手好用。

- 已完成：增加全局仪表盘，展示岗位总数、待处理 JD、待尽调、推荐岗位、风险岗位、疑似过期岗位。
- 已完成：仪表盘提供下一步建议，可直接跳转岗位、尽调、排序模块。
- 已完成：仪表盘增加 30 天趋势图和数据质量体检，用户可以直接从问题卡片跳转到对应模块处理。
- 已完成：求职 CRM 看板支持拖拽和下拉修改状态，减少在岗位列表和总览之间反复切换。
- 已完成：数据质量体检增加一键修复维护标签，降低后续补 JD、清理噪音、归档过期岗位的操作成本。
- 已完成：支持保存常用搜索条件和岗位筛选组合，包含 BOSS 抓取参数与本地筛选条件。
- 已完成：批量导出已覆盖岗位、尽调、排序结果；批量尽调、批量排序已有主流程入口。
- 已完成：岗位列表支持卡片、紧凑、表格三种视图切换。
- 已完成：PDF 模板可按岗位类型自动推荐。
- 已完成：简历优化支持版本保存与版本对比。
- 已完成：PDF 模板视觉继续打磨，降低正文密集感，AI 深度报告导出更接近正式复盘报告。
- 已完成：维护区增加 PDF 真实渲染检查，样例简历和 AI 报告会转成图片并检测非空白比例。
- 已完成：PDF 渲染检查结果返回缩略图，前端可直接预览真实渲染页面。
- 已完成：设置页增加生产/演示/测试数据模式切换，生产模式默认禁止示例抓取，环境变量锁定时页面会提示不可覆盖。
- 已完成：新增首次使用向导增强接口，返回下一步主动作、目标模块、步骤阻塞原因和灰度发送提示。
- 已完成：设置页增加岗位导入向导，支持 CSV/JSON 预览导入，正式导入前展示新增、重复和无效数据数量。
- 已完成：岗位导入向导增加标准 CSV 模板下载，降低字段填写错误。

## 阶段 5：智能化增强（首轮完成）

目标：让 AI 从内容生成工具升级为求职助理。

- 已完成：自动生成投递策略，输出优先投递、观察推进、暂不推进、需补充信息，并给出置信度和下一步动作。
- 已完成：识别 JD 噪音和真实性，能标记营销文案、页面噪音、职责缺失、信息密度不足。
- 已完成：按岗位反推简历改写建议，突出命中关键词、缺失关键词和可改写 bullet。
- 已完成：生成面试准备，包含公司背景、面试问题、回答角度、反问问题。
- 已完成：增加跟进提醒，基于已打招呼、已投递、面试中等状态提示后续动作。
- 已完成：增加风险解释，将工商异常、处罚、被执行、负面舆情转成用户可理解的问题、影响和面试追问。
- 已完成：仪表盘接入智能求职助理面板，默认分析当前已选岗位或岗位池第一条。
- 已完成：智能助理结果已持久化，可按岗位读取历史建议。
- 已完成：智能助理增加 AI 深度报告接口，有 AI Key 时使用 AI 生成，无 AI Key 时规则兜底。
- 已完成：AI 深度报告接入用户偏好，稳定性、薪资、成长、匹配度、规避行业和偏好城市会参与个性化建议。
- 已完成：AI 深度报告支持 Markdown / JSON / PDF 导出。
- 已完成：AI 深度报告支持人工复核保存和版本记录。
- 已完成：AI 深度报告支持章节级人工编辑，覆盖总结、策略、匹配、风险、面试准备和行动建议，并同步进入 Markdown/PDF 导出。
- 已完成：AI 深度报告可导出正式 PDF，前端提供下载入口。
- 已完成：AI 建议反馈闭环首版，用户可对关键 AI 输出快速标记“有用/需改”，系统保留最近反馈用于后续策略优化。
- 已完成：AI 提示词版本管理首版，深度报告会保存提示词版本、反馈快照和输入摘要，便于比较不同版本的输出质量。
- 已完成：自动打招呼最终确认摘要，真实自动发送前展示岗位、链接、话术长度、剩余额度和风险项。
- 已完成：真实自动打招呼增加灰度模式，默认要求当天先成功发送 1 个岗位，再开放批量真实发送。
- 已完成：打招呼模块增加失败恢复台，按页面风控、页面结构变化、登录异常、网络失败、话术校验等原因分组，并给出处理建议。

## 外部服务与存储操作说明

- 生产运行不设置 `BOSS_WORKBENCH_TEST_MODE`；该变量只由 Playwright 临时后端进程使用。
- 外部调用只对网络、限流和 5xx 等瞬时错误重试；鉴权、参数和配置错误不会重复请求。
- 真实 BOSS 自动发送仅执行正常浏览器页面操作；遇到未登录、Cookie 失效、验证码、页面风控或页面结构变化会停止，不做绕过。
- 设置页的“创建数据库备份”只生成 SQLite 副本；“预览最近备份”只做完整性检查，不会自动替换当前数据库。
- SQLite 主存储切换前建议执行完整数据备份；JSON 副本仍用于兼容回退，不代表可以跳过备份。

## 阶段 6：上线与安全

目标：让项目更安全、更可交付。

- 已完成：增加本地访问保护，防止局域网或恶意页面读取配置。
- 已完成：完整密钥导出增加一次性确认令牌。
- 已完成：API Key 加密存储，避免明文 JSON 长期保存。
- 已完成：增加 release 校验脚本，覆盖后端测试、前端验证、类型检查、生产构建和 Git 差异格式检查。
- 已完成：release 校验脚本增加本地明文密钥扫描。
- 已完成：维护模块增加 release manifest 和 security audit。
- 已完成：启动器预检脚本和 Playwright 冒烟测试基础。
- 已完成：维护区增加上线人工验收清单，覆盖核心求职流程、打招呼流程和上线安全检查。
- 已完成：维护区增加上线验收包，整合上线体检、生产保护、隐私扫描、人工验收清单和版本快照。
- 已完成：维护区增加隐私扫描入口，扫描简历、上传、岗位、招呼语、尽调和 AI 报告中的手机号、邮箱、身份证号等明显个人信息。
- 已完成：维护区增加版本快照，汇总版本号、提交号、质量门禁、本版亮点和已知风险。
- 已完成：维护区增加数据清理预演，只展示将被归档/清理的数据类型、数量和样例，不直接删除数据。
- 已完成：维护区增加错误诊断中心，将运行环境、BOSS 登录、工商 API、百度搜索、AI 配置、PDF 渲染、存储和打招呼失败集中展示。
- 已完成：错误诊断中心增加结构化修复动作，支持跳转目标模块提示、刷新检查、导出脱敏备份等操作。
- 已完成：维护区增加一键发布前检查，聚合诊断中心、隐私扫描、清理预演、备份、存储和手动测试门禁。
- 已完成：数据清理预演增加确认清理入口，执行时归档疑似过期岗位、失败任务和旧简历对话，并记录操作日志。
- 已完成：维护区增加 Release Record，保存版本号、发布人、上线决策、版本快照和发布检查结果，方便后续追溯。
- 已完成：新增帮助中心，按仪表盘、简历、岗位、尽调、排序、打招呼、设置维护说明用途、适用场景、推荐步骤、完成信号、常见失败、安全提醒和修复动作。
- 已完成：帮助中心增加 FAQ 和术语解释，覆盖灰度模式、一键尽调、岗位池质量、发布记录等关键概念。
- 已完成：顶部导航增加帮助入口，Playwright 覆盖帮助中心打开、打招呼修复动作、推荐步骤和术语展示。
- 已完成：帮助中心增加接口失败兜底，接口 404/后端未重启/网络失败时展示内置帮助、失败原因和重试入口，避免页面一直停留在加载中。
- 已完成：Playwright 冒烟测试增加设置页诊断中心、清理预演和岗位导入模板入口覆盖。
- 已完成：完整数据维护增加脱敏备份导出，隐藏常见密钥、手机号和邮箱，并跳过无法可靠脱敏的二进制文件。
- 已完成：维护区增加版本说明，展示版本号、阶段、本版亮点和上线注意事项。
- 已完成：维护区增加真实依赖审计入口，支持前端 `pnpm audit` 与后端 `pip-audit` 可用性/执行结果摘要。
- 已完成：维护区增加 SQLite 数据迁移向导，拆分备份、迁移、校验、设主存储和回滚路径状态。
- 已完成：增加 macOS 桌面端基础打包，Electron 内置目录式后端运行包、前端构建产物、Python 运行依赖和 Chromium 浏览器运行时，用户数据迁入系统应用数据目录，并减少单文件解包导致的启动等待。
- 持续项：release 打包仍可继续增加代码签名、公证、应用图标、自动更新和跨平台安装包封装。
- 持续项：自动化质量门禁后续可继续接入更完整的真实浏览器抓取和失败恢复测试。

## 优先级建议

近期优先处理：

1. 为桌面端补充正式应用图标、代码签名、公证和自动更新策略。
2. 为真实 BOSS 链路增加人工验收记录，保存验收人、时间、结果和备注。
3. PDF 模板继续补充更多岗位类型的版式，并增加真实页面级视觉回归截图。
4. 扩展 Playwright 覆盖真实浏览器抓取和失败恢复链路。
5. release 打包继续增加版本号、变更记录和跨平台封装。

## 开发原则

- 优先保证完整流程稳定，再扩展新功能。
- 新增功能必须接入状态显示、错误提示和恢复动作。
- 涉及岗位、简历、尽调、密钥的数据变更必须考虑备份与清理。
- 前端改动需通过类型检查、测试和生产构建。
- 后端改动需补充回归测试，避免只依赖人工验证。
